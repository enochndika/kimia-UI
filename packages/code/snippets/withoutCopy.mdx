```jsx
export const CodePage = () => (
  <div className="m-8">
    <Code>yarn add next react react-dom</Code>
    <div className="mb-5" />
    <Code>npm install next react react-dom</Code>
  </div>
);

/* logic */
const Code = ({ children }) => (
  <pre
    className={`inline-flex font-mono text-sm py-2.5 px-4 rounded overflow-x-auto max-w-full bg-gray-100 text-black border`}
  >
    <span>{children}</span>
  </pre>
);
```
